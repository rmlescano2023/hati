.DS_Store
node_modules
dist
dist-ssr
.vite
*.local

# Secrets pulled by `vercel env pull`. The example is intentionally tracked,
# so the negation must come after the catch-all to survive it.
.env*
!.env.example

.vercel
*.log
